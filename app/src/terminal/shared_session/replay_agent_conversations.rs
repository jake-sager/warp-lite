use crate::ai::agent::conversation::AIConversation;
use crate::terminal::shared_session::ai_agent::ResponseEvent;

pub fn reconstruct_response_events_from_conversations(
    _conversations: &[AIConversation],
) -> Vec<ResponseEvent> {
    Vec::new()
}
