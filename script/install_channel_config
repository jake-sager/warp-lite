#!/usr/bin/env bash
#
# Warp Lite does not use Warp's private channel-config generator.
# The OSS binary builds with local-only channel configuration from
# app/src/bin/oss.rs.

set -euo pipefail

echo "warp-channel-config is not used by warp-lite; nothing to install."
